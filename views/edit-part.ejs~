<% include partials/header.ejs %>
<div class="container">
    <% if (message) { %>
        <p class="text-center text-danger"><%= message %></p>
    <% } %>

    <% if (part) { %>
        <form class="add-part-form" action="" method="post" enctype="multipart/form-data">
            <div class="form-row">
                <div class="form-group col-md-4">
                    <label for="station_id">Station ID</label>
                    <input type="text" class="form-control" name="station_id" id="station_id" value="<%= part.station_id %>" required>
                </div>
                <div class="form-group col-md-4">
                    <label for="qty">Qty.</label>
                    <input type="number" class="form-control" name="qty" id="qty" value="<%= part.qty %>" required>
                </div>
                <div class="form-group col-md-4">
                    <label for="manufacturer">Manufacturer</label>
                    <input type="text" class="form-control" name="manufacturer" id="manufacturer" value="<%= part.manufacturer %>" required>
                </div>
            </div>
            <div class="form-row">
                <div class="form-group col-md-6">
                    <label for="part_number">Part Number</label>
                    <input type="text" class="form-control" name="part_number" id="part_number" placeholder="Part Number" value="<%= part.part_number %>" required>
                </div>
                <div class="form-group col-md-6">
                    <label for="status">Status</label>
                    <select id="status" name="status" class="form-control" required>
                        <option selected><%= part.status %></option>
                        <option>Open</option>
                        <option>Closed</option>
                    </select>
                </div>
		<div class="form-group col-md-6">
		  <label for="description">Description</label>
		  <input type="textarea" class="form-control" name="description" id="description" placeholder="Description" value="<%= part.description %>" required>
		</div>
            </div>
            <button type="submit" class="btn btn-success float-right">Update Part</button>
        </form>
    <% } else { %>
        <p class="text-center">Part Not Found. Go <a href="/add">here</a> to add parts.</p>
    <% } %>
</div>
</div>
</body>
</html>
